"""NeuroDB Tier 0 completion benchmark.

The *existential* benchmark: does NeuroDB's pattern completion actually beat
trivial sklearn imputers on multimodal data? See ``protocol.run_benchmark`` and
``run_benchmark.py``. This package is benchmark-only and is not imported by the
NeuroDB runtime (``requirements-bench.txt``, not ``requirements.txt``).
"""
